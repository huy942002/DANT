package com.model.DAO;

import com.model.entities.TotalCategory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TotalCategoryDAO extends JpaRepository<TotalCategory,Integer> {
}
